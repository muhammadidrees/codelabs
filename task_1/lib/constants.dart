import 'package:flutter/material.dart';

enum status{
  pending, 
  wroking, 
  resolved,
}

Color pending_status = Color(0xFFF1592A);
Color working_status = Color(0xFFFCB040);
Color resolved_status = Color(0xFFE7E7E9);